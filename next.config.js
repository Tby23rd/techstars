const nextConfig = {
    images: {

    },
    typescript: {
        tsconfig: {
            target: 'es2017',
            // Other compiler options...
        },
    },
};

module.exports = nextConfig;