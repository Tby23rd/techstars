const nextConfig = {
    images: {
        domains: ['www.thecocktaildb.com'],
    },
    typescript: {
        tsconfig: {
            target: 'es2017',
            // Other compiler options...
        },
    },
};

module.exports = nextConfig;